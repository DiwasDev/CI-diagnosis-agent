Make the CI-diagnosis-agent repo preprint-ready (no paper writing yet — just repair to the point where a coding agent can safely write the IJCAI-style 2-3 pager):

1. Fix the reproducibility bug in `experiments/policies/base.py` (restore `expected_information_gain` to module level), then verify `run_policy_comparison.py`, `run_derived_threshold_policy.py`, and `run_failure_analysis.py` all run at HEAD and reproduce the README numbers exactly.

2. Fix false/misleading README claims:
   - "P4 misclassifies only 5 of 500" → correct to 185/500 failures.
   - Disclose that the 500-case benchmark is synthetic, generated from the agent's own priors/likelihoods (the circularity the brief's §25.2 requires stating).
   - "real costs" → "assumed cost model (see decisions/costs.md)"; "on real data" → corrected phrasing.

3. Resolve the doc/code model mismatch: update `decisions/probability_decision_record.md` §13 (or constants) so E4/E3 likelihood tables match `experiments/constants.py`, with a dated note recording the change.

4. Fix citations: resolve "Zheng et al. 2025" to a real, checkable reference or remove it (code + mapping.md); either source the "1 in 7 CI runs / 30 min" claim or relabel it as an assumption.

5. Scrub coursework framing: README.md:172, run_derived_threshold_policy.py:4 docstring, failure_analysis.md headers, generalization.md exam-style Q&A (reframe as "Background"), notebook markdown quoting the lecture PDF; remove the lecture PDF from the repo directory; move reddit-discussions.md out of the repo (keep locally).

6. Clear stale/contradictory artifacts: regenerate or strip notebook outputs (agent.ipynb shows pre-fix P4 numbers); fix the $8.3 typo in costs.md; fix the stale "100 test cases / old filename" audit entry in the decision record.

7. Cheap rigor upgrades: run the policy comparison at seeds 42/7/123 and commit per-case results (JSON) + a summary table; make the P0 baseline not fit on the evaluation cases (or disclose it); add a failure analysis for the derived-threshold (Fix 3) policy.

Deliverable: a repo whose numbers, docs, and code all agree, is reproducible at HEAD, has variance reported, and contains no false claims, unverifiable citations, or coursework leaks — ready to hand to the paper-writing step (which is a separate task, per the one-intent-per-change rule).